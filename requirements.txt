# Minimal runtime dependencies for test4_smp.py / test4_smp_hpc.py
# Note: install the CUDA-matched PyTorch build for the target cluster.
# Example for CUDA 12.1:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

numpy>=1.24,<3
pandas>=2.0,<3
matplotlib>=3.7,<4
scikit-learn>=1.3,<2
h5py>=3.9,<4
torch>=2.1,<3
